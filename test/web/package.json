{
  "name": "browserify-test",
  "scripts": {
    "build": "browserify -r backbone-orm -r backbone > browserify-bundle.js"
  },
  "devDependencies": {
    "browserify": "*",
    "brfs": "*"
  },
  "dependencies": {
    "jquery": "*",
    "underscore": "1.5.x",
    "backbone": ">=1.0.0",
    "moment": "2.x.x",
    "inflection": "1.x.x",
    "lru-cache": "2.3.x",
    "backbone-orm": "git+https://github.com/vidigami/backbone-orm.git"
  }
}
